source "https://rubygems.org"

# Mesma stack que o GitHub Pages usa para construir o site.
gem "github-pages", group: :jekyll_plugins

# Necessário para `jekyll serve` no Ruby 3+
gem "webrick", "~> 1.8"
